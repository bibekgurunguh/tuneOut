import '@testing-library/jest-dom/extend-expect';
import '@testing-library/jest-dom';

describe('handleCapture Function', () => {
  beforeEach(() => {
    
  });
  it('should record the stream and return an appropriate promise', () => {
    // const trackId = ;
    // expect(trackId)
  });
});