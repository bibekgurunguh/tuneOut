import React from 'react';
import ReactDOM from 'react-dom';
import './index-css/index-options.css';
import Options from './components/Options';

ReactDOM.render(
  <React.StrictMode>
    <Options/>
  </React.StrictMode>,
  document.getElementById('root')
);

